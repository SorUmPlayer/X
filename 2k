--[[
    ⚽ Blue Lock Rivals – REACH + Desempenho + Correções
    WindUI: ukinabr (mesma do Project Sor / OminiX)
]]

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local Workspace = game:GetService("Workspace")
local CoreGui = game:GetService("CoreGui")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local HttpService = game:GetService("HttpService")
local Lighting = game:GetService("Lighting")
local Stats = game:GetService("Stats")
local StarterGui = game:GetService("StarterGui")

local player = Players.LocalPlayer
local camera = Workspace.CurrentCamera

print("[Blue Lock] Script iniciado")

-- =============================================
-- AUTO SAVE
-- =============================================
local CONFIG_FILE = "BlueLock_AutoSave.json"

local function LoadConfig()
    if not isfile then return {} end
    if isfile(CONFIG_FILE) then
        local success, data = pcall(function()
            return HttpService:JSONDecode(readfile(CONFIG_FILE))
        end)
        if success and type(data) == "table" then return data end
    end
    return {}
end

local function SaveConfigToFile(configTable)
    if not writefile then return end
    local success, json = pcall(function()
        return HttpService:JSONEncode(configTable)
    end)
    if success then pcall(function() writefile(CONFIG_FILE, json) end) end
end

local Config = {
    velocidadeBola = 65,
    forcaBolaVelocidade = 100,
    aimbotDelayValue = 0,
    reachPersonagemTamanho = 10,
    reachBolaTamanho = 20,
    curvasIntensidade = 5,
    botaoFlutuante = false,
    aimbotPersonagem = false,
    aimbotCamera = false,
    botaoCamera = false,
    visaoBola = false,
    advinhaChute = false,
    reachPersonagem = false,
    reachBola = false,
    mostrarESP = false,
    forcaBola = false,
    curvasAtivo = false,
    dribleAtivo = false,
    statsToggle = false,
    fpsModeAtivo = false,
    fpsModeLevel = 0,
    pingMenos = false,
    timeSelecionado = "Home",
}
local savedConfig = LoadConfig()
for k, v in pairs(savedConfig) do Config[k] = v end

local function SaveConfig() SaveConfigToFile(Config) end
task.spawn(function()
    while true do task.wait(5) SaveConfig() end
end)

-- =============================================
-- FUNÇÃO SEGURA DE PARENT
-- =============================================
local function safeParent(gui)
    local ok = pcall(function()
        if gethui then gui.Parent = gethui()
        elseif syn and syn.protect_gui then syn.protect_gui(gui); gui.Parent = CoreGui
        else gui.Parent = CoreGui end
    end)
    if not ok then
        pcall(function() gui.Parent = player:WaitForChild("PlayerGui") end)
    end
end

-- =============================================
-- WINDUI (ukinabr – mesma do OminiX)
-- =============================================
local WindUI = nil

print("[Blue Lock] Carregando WindUI...")

local function loadWindUI()
    -- Tentativa 1: URL ukinabr (mesma do OminiX)
    local url1 = "https://raw.githubusercontent.com/ukinabr/WindUI/refs/heads/main/dist/main.lua"
    local ok, res = pcall(function()
        return loadstring(game:HttpGet(url1))()
    end)
    if ok and res then
        print("[Blue Lock] WindUI carregada (ukinabr)")
        return res
    end

    -- Tentativa 2: Footagesus releases
    local url2 = "https://github.com/Footagesus/WindUI/releases/latest/download/main.lua"
    ok, res = pcall(function()
        return loadstring(game:HttpGet(url2))()
    end)
    if ok and res then
        print("[Blue Lock] WindUI carregada (Footagesus fallback)")
        return res
    end

    return nil
end

WindUI = loadWindUI()

if not WindUI then
    pcall(function()
        StarterGui:SetCore("SendNotification", {
            Title = "Erro",
            Text = "Não foi possível carregar a WindUI.",
            Duration = 5
        })
    end)
    print("[Blue Lock] ERRO: WindUI não carregou")
    return
end

-- =============================================
-- CRIAÇÃO DA JANELA
-- =============================================
local Window = WindUI:CreateWindow({
    Title = "⚽ Blue Lock Rivals",
    Icon = "rbxassetid://135710581431595",
    Author = "Sor_Gostoso",
    Folder = "BlueLock",
    Size = UDim2.fromOffset(500, 700),
    Transparent = false,
    Theme = "Dark",
    Resizable = true,
    SideBarWidth = 200,
    BackgroundImageTransparency = 0.42,
    HideSearchBar = true,
    ScrollBarEnabled = false,
    User = {
        Enabled = true,
        Anonymous = false,
        Callback = function() print("clicked") end,
    },
})

print("[Blue Lock] Janela criada com sucesso")

-- =============================================
-- DETECÇÃO DA BOLA
-- =============================================
local bola = nil
local hitbox = nil
local bolaValida = false

local function atualizarReferenciaBola()
    for _, child in ipairs(Workspace:GetChildren()) do
        if child:IsA("Model") then
            local football = child:FindFirstChild("Football")
            if football then
                bola = football
                local hb = bola:FindFirstChild("Hitbox")
                hitbox = (hb and hb:IsA("BasePart")) and hb or bola
                bolaValida = true
                return
            end
        elseif child.Name == "Football" then
            bola = child
            local hb = bola:FindFirstChild("Hitbox")
            hitbox = (hb and hb:IsA("BasePart")) and hb or bola
            bolaValida = true
            return
        end
    end
    bola = nil; hitbox = nil; bolaValida = false
end

Workspace.ChildAdded:Connect(atualizarReferenciaBola)
Workspace.ChildRemoved:Connect(atualizarReferenciaBola)
atualizarReferenciaBola()

task.spawn(function()
    while true do
        task.wait(1)
        if not bolaValida or not hitbox then atualizarReferenciaBola() end
    end
end)

local function getBall()
    if bola and bolaValida then return bola end
    local football = Workspace:FindFirstChild("Football")
    if football then
        local ballAnims = football:FindFirstChild("BallAnims")
        if ballAnims then
            if ballAnims:IsA("BasePart") then return ballAnims end
            return ballAnims:FindFirstChildWhichIsA("BasePart") or ballAnims.PrimaryPart
        end
    end
    return nil
end

local function euTenhoBola()
    local meuModel = Workspace:FindFirstChild(player.Name)
    if meuModel and meuModel:FindFirstChild("Football") then return true end
    return false
end

local function getMyTeam()
    local values = player:FindFirstChild("Values")
    if values then
        local teamVal = values:FindFirstChild("Team")
        if teamVal then return tostring(teamVal.Value) end
    end
    return nil
end

-- =============================================
-- VARIÁVEIS
-- =============================================
local aimbotPersonagem = Config.aimbotPersonagem
local aimbotCamera = Config.aimbotCamera
local reachBolaAtivo = Config.reachBola
local reachBolaDistancia = Config.reachBolaTamanho
local mostrarESP = Config.mostrarESP

local reachPersonagemAtivo = Config.reachPersonagem
local reachPersonagemTamanho = Config.reachPersonagemTamanho

local forcaBolaEnabled = Config.forcaBola
local forcaBolaVelocidade = Config.forcaBolaVelocidade
local tinhaBola = false

local curvasEnabled = Config.curvasAtivo
local curvasModo = 1
local curvasIntensidade = Config.curvasIntensidade
local curvasBotaoGUI, curvasBotao = nil, nil
local curvasTimer, curvasIntervalo, curvasDirecaoAtual = 0, 1.0, 1

local dribleEnabled = Config.dribleAtivo
local dribleSphere = nil
local dribleLastFired = 0
local dribleCooldown = 0.3
local dribleAnimId = "18668814876"
local dribleRadius = 70
local driblePlayingAnims = {}
local dribleEvent = nil

local velocidadeBola = Config.velocidadeBola
local isControlActive = false
local controlConnection, controlScreenGui, toggleButton = nil, nil, nil

local cameraButtonActive = false
local cameraScreenGui, cameraToggleButton = nil, nil

local aimbotDelayValues = {0, 0.1, 0.2, 0.25, 0.40, 0.60, 0.70, 1.0, 2.0, 3.0, 5.0}
local aimbotDelayValue = Config.aimbotDelayValue
local aimbotBlocked = false
local aimbotBlockedUntil = 0

local visaoBolaEnabled = Config.visaoBola
local advinhaChuteEnabled = Config.advinhaChute
local linhaVisao, linhaChute, circuloChute = nil, nil, nil

local gyro = nil
local selectedTeam = Config.timeSelecionado
local espSphere = nil
local hitboxOriginalSize = Vector3.new(1.4, 1.4, 1.4)
local hitboxOriginalTransparency = 0

-- =============================================
-- FUNÇÕES
-- =============================================
local function shouldAimbotWork()
    if euTenhoBola() then
        aimbotBlocked = true
        aimbotBlockedUntil = tick() + aimbotDelayValue
        return false
    end
    if aimbotBlocked then
        if tick() >= aimbotBlockedUntil then aimbotBlocked = false; return true
        else return false end
    end
    return true
end

local function aplicarReachBola()
    if not hitbox then return end
    local tenho = euTenhoBola()
    local tamanhoDesejado, transparenciaDesejada
    if tenho or not reachBolaAtivo then
        tamanhoDesejado = hitboxOriginalSize
        transparenciaDesejada = hitboxOriginalTransparency
    else
        tamanhoDesejado = Vector3.new(reachBolaDistancia, reachBolaDistancia, reachBolaDistancia)
        transparenciaDesejada = 1
    end
    if hitbox.Size ~= tamanhoDesejado or hitbox.Transparency ~= transparenciaDesejada then
        hitbox.Size = tamanhoDesejado
        hitbox.Transparency = transparenciaDesejada
    end
end

local function aplicarReachPersonagem()
    if not reachPersonagemAtivo then return end
    if euTenhoBola() then return end
    if not hitbox or not bolaValida then return end
    local char = player.Character
    if not char then return end
    local hrp = char:FindFirstChild("HumanoidRootPart")
    if not hrp then return end
    local dist = (hrp.Position - hitbox.Position).Magnitude
    if dist <= reachPersonagemTamanho then
        if firetouchinterest then
            for i = 1, 3 do
                firetouchinterest(hrp, hitbox, 0)
                task.wait(0.005)
                firetouchinterest(hrp, hitbox, 1)
            end
        end
    end
end

local function aplicarForcaBola()
    if not forcaBolaEnabled then return end
    if not bola or not bolaValida then return end
    local tenho = euTenhoBola()
    if tinhaBola and not tenho then
        local dir = camera.CFrame.LookVector
        dir = Vector3.new(dir.X, 0, dir.Z)
        if dir.Magnitude < 0.1 then dir = Vector3.new(1, 0, 0) else dir = dir.Unit end
        local vel = dir * forcaBolaVelocidade + Vector3.new(0, 15, 0)
        bola.AssemblyLinearVelocity = vel
    end
    tinhaBola = tenho
end

local function aplicarCurvas()
    if not curvasEnabled then return end
    if not bola or not bolaValida then return end
    if euTenhoBola() then return end
    local vel = bola.AssemblyLinearVelocity
    local speed = vel.Magnitude
    if speed < 40 then return end
    if bola.Position.Y < 0.5 then return end
    local dir = Vector3.new(vel.X, 0, vel.Z)
    if dir.Magnitude < 0.01 then return end
    dir = dir.Unit
    local perp = Vector3.new(-dir.Z, 0, dir.X)
    local fator = (curvasIntensidade / 10) * 15 + 1.5
    local lado = 1
    if curvasModo == 1 then lado = 1
    elseif curvasModo == 2 then lado = -1
    elseif curvasModo == 3 then
        local now = tick()
        if now - curvasTimer > curvasIntervalo then
            curvasTimer = now
            curvasDirecaoAtual = (curvasDirecaoAtual == 1) and -1 or 1
            curvasIntervalo = 0.5 + math.random() * 0.5
        end
        lado = curvasDirecaoAtual
    end
    bola.AssemblyLinearVelocity = vel + perp * lado * fator
end

-- Drible
local function setupDrible()
    if not dribleEvent then
        pcall(function()
            local packages = ReplicatedStorage:FindFirstChild("Packages")
            if packages then
                local knit = packages:FindFirstChild("Knit")
                if knit then
                    local services = knit:FindFirstChild("Services")
                    if services then
                        local ballService = services:FindFirstChild("BallService")
                        if ballService then
                            local re = ballService:FindFirstChild("RE")
                            if re then dribleEvent = re:FindFirstChild("Dribble") end
                        end
                    end
                end
            end
        end)
    end
    if not dribleSphere then
        dribleSphere = Instance.new("Part")
        dribleSphere.Name = "DetectionSphereVisual"
        dribleSphere.Shape = Enum.PartType.Ball
        dribleSphere.Size = Vector3.new(dribleRadius * 2, dribleRadius * 2, dribleRadius * 2)
        dribleSphere.Material = Enum.Material.ForceField
        dribleSphere.Color = Color3.fromRGB(255, 0, 128)
        dribleSphere.Transparency = 0.6
        dribleSphere.CanCollide = false
        dribleSphere.Anchored = false
        dribleSphere.CastShadow = false
        dribleSphere.Parent = workspace
    end
end

local function trackDribleAnimation(character)
    local humanoid = character:FindFirstChild("Humanoid")
    if not humanoid then return end
    local animator = humanoid:FindFirstChild("Animator") or humanoid
    local function checkTrack(track)
        if not track or not track.Animation then return end
        if string.find(tostring(track.Animation.AnimationId), dribleAnimId) then
            driblePlayingAnims[character] = true
            local cs, ce
            cs = track.Stopped:Connect(function() driblePlayingAnims[character] = nil; cs:Disconnect(); if ce then ce:Disconnect() end end)
            ce = track.Ended:Connect(function() driblePlayingAnims[character] = nil; cs:Disconnect(); ce:Disconnect() end)
        end
    end
    for _, track in ipairs(animator:GetPlayingAnimationTracks()) do checkTrack(track) end
    animator.AnimationPlayed:Connect(checkTrack)
end

local function setupDriblePlayerTracking(plr)
    if plr == player then return end
    if plr.Character then task.spawn(trackDribleAnimation, plr.Character) end
    plr.CharacterAdded:Connect(function(char)
        driblePlayingAnims[char] = nil
        task.spawn(trackDribleAnimation, char)
    end)
end

local function isDribleAnimActive(character)
    if driblePlayingAnims[character] then return true end
    local humanoid = character:FindFirstChild("Humanoid")
    if not humanoid then return false end
    local animator = humanoid:FindFirstChild("Animator") or humanoid
    for _, track in ipairs(animator:GetPlayingAnimationTracks()) do
        if track.Animation and string.find(tostring(track.Animation.AnimationId), dribleAnimId) then return true end
    end
    return false
end

local function isLookingAtLocal(targetHrp, localHrp)
    local dir = (localHrp.Position - targetHrp.Position).Unit
    return targetHrp.CFrame.LookVector:Dot(dir) >= 0.5
end

local function aplicarDrible()
    if not dribleEnabled then
        if dribleSphere then dribleSphere.Transparency = 1 end
        return
    end
    local char = player.Character
    if not char then return end
    local hrp = char:FindFirstChild("HumanoidRootPart")
    if not hrp then return end
    if dribleSphere then
        dribleSphere.Position = hrp.Position
        dribleSphere.Transparency = 0.6
        dribleSphere.Parent = workspace
    end
    local now = tick()
    if now - dribleLastFired < dribleCooldown then return end
    for _, plr in ipairs(Players:GetPlayers()) do
        if plr ~= player and plr.Character then
            local targetHrp = plr.Character:FindFirstChild("HumanoidRootPart")
            if targetHrp then
                local dist = (targetHrp.Position - hrp.Position).Magnitude
                if dist <= dribleRadius and isLookingAtLocal(targetHrp, hrp) and isDribleAnimActive(plr.Character) then
                    dribleLastFired = now
                    if dribleEvent then dribleEvent:FireServer("fbc7892b8f694cfd98d7ef93bc018abe", 3) end
                    break
                end
            end
        end
    end
end

for _, plr in ipairs(Players:GetPlayers()) do setupDriblePlayerTracking(plr) end
Players.PlayerAdded:Connect(setupDriblePlayerTracking)
Players.PlayerRemoving:Connect(function(plr) if plr.Character then driblePlayingAnims[plr.Character] = nil end end)

-- =============================================
-- CONTROLE DE BOLA
-- =============================================
local function resetCamera()
    if player.Character and player.Character:FindFirstChild("Humanoid") then
        camera.CameraSubject = player.Character.Humanoid
    end
end

local function toggleControl()
    isControlActive = not isControlActive
    if isControlActive then
        if toggleButton then
            toggleButton.BackgroundColor3 = Color3.fromRGB(40, 167, 69)
            toggleButton.Text = "Control BALL [ON]"
        end
        if controlConnection then controlConnection:Disconnect() end
        controlConnection = RunService.Heartbeat:Connect(function()
            local ball = getBall()
            if ball then
                if euTenhoBola() then resetCamera()
                else
                    camera.CameraSubject = ball
                    ball.AssemblyLinearVelocity = camera.CFrame.LookVector * velocidadeBola
                end
            else resetCamera() end
        end)
    else
        if toggleButton then
            toggleButton.BackgroundColor3 = Color3.fromRGB(220, 53, 69)
            toggleButton.Text = "Control BALL [OFF]"
        end
        if controlConnection then controlConnection:Disconnect(); controlConnection = nil end
        resetCamera()
    end
end

local function criarBotaoFlutuante()
    if controlScreenGui then return end
    local screenGui = Instance.new("ScreenGui")
    screenGui.Name = "ControlBallGui"; screenGui.ResetOnSpawn = false
    safeParent(screenGui)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0, 180, 0, 45); btn.Position = UDim2.new(0.5, -90, 0.1, 0)
    btn.BackgroundColor3 = Color3.fromRGB(220, 53, 69); btn.Text = "Control BALL [OFF]"
    btn.TextColor3 = Color3.fromRGB(255, 255, 255); btn.Font = Enum.Font.GothamBold
    btn.TextSize = 16; btn.Active = true; btn.Draggable = true; btn.Parent = screenGui
    local uiCorner = Instance.new("UICorner"); uiCorner.CornerRadius = UDim.new(0, 8); uiCorner.Parent = btn
    btn.MouseButton1Click:Connect(toggleControl)
    toggleButton = btn; controlScreenGui = screenGui
end

local function removerBotaoFlutuante()
    if isControlActive then toggleControl() end
    if controlScreenGui then controlScreenGui:Destroy(); controlScreenGui = nil; toggleButton = nil end
end

local function toggleCameraButton()
    cameraButtonActive = not cameraButtonActive
    if cameraButtonActive then
        if cameraToggleButton then
            cameraToggleButton.BackgroundColor3 = Color3.fromRGB(40, 167, 69)
            cameraToggleButton.Text = "Aimbot Cam [ON]"
        end
        aimbotCamera = true
    else
        if cameraToggleButton then
            cameraToggleButton.BackgroundColor3 = Color3.fromRGB(220, 53, 69)
            cameraToggleButton.Text = "Aimbot Cam [OFF]"
        end
        aimbotCamera = false
    end
end

local function criarBotaoCameraFlutuante()
    if cameraScreenGui then return end
    local screenGui = Instance.new("ScreenGui")
    screenGui.Name = "CameraAimbotGui"; screenGui.ResetOnSpawn = false
    safeParent(screenGui)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0, 180, 0, 45); btn.Position = UDim2.new(0.5, -90, 0.15, 0)
    btn.BackgroundColor3 = Color3.fromRGB(220, 53, 69); btn.Text = "Aimbot Cam [OFF]"
    btn.TextColor3 = Color3.fromRGB(255, 255, 255); btn.Font = Enum.Font.GothamBold
    btn.TextSize = 16; btn.Active = true; btn.Draggable = true; btn.Parent = screenGui
    local uiCorner = Instance.new("UICorner"); uiCorner.CornerRadius = UDim.new(0, 8); uiCorner.Parent = btn
    btn.MouseButton1Click:Connect(toggleCameraButton)
    cameraToggleButton = btn; cameraScreenGui = screenGui
end

local function removerBotaoCameraFlutuante()
    if cameraButtonActive then toggleCameraButton() end
    if cameraScreenGui then cameraScreenGui:Destroy(); cameraScreenGui = nil; cameraToggleButton = nil end
end

-- =============================================
-- VISÃO DE JOGO
-- =============================================
local function getInimigoComBola()
    local myTeam = getMyTeam()
    if not myTeam then return nil end
    for _, outro in ipairs(Players:GetPlayers()) do
        if outro ~= player and outro.Character and outro.Character:FindFirstChild("Football") then
            local values = outro:FindFirstChild("Values")
            if values then
                local teamVal = values:FindFirstChild("Team")
                if teamVal and tostring(teamVal.Value) ~= myTeam then return outro end
            end
        end
    end
    return nil
end

local function criarLinhaVisao()
    if not linhaVisao or not linhaVisao.Parent then
        if linhaVisao then linhaVisao:Destroy() end
        linhaVisao = Instance.new("Part")
        linhaVisao.Name = "LinhaVisao"; linhaVisao.Anchored = true; linhaVisao.CanCollide = false
        linhaVisao.Transparency = 0.1; linhaVisao.Color = Color3.fromRGB(0, 255, 0)
        linhaVisao.Material = Enum.Material.Neon; linhaVisao.Size = Vector3.new(1.0, 1.0, 100)
        linhaVisao.Parent = workspace
    end
    return linhaVisao
end

local function atualizarVisaoBola()
    if not visaoBolaEnabled then
        if linhaVisao then linhaVisao:Destroy(); linhaVisao = nil end
        return
    end
    local inimigo = getInimigoComBola()
    if not inimigo then
        if linhaVisao then linhaVisao:Destroy(); linhaVisao = nil end
        return
    end
    local char = inimigo.Character
    if not char then return end
    local head = char:FindFirstChild("Head")
    local hrp = char:FindFirstChild("HumanoidRootPart")
    local origem = head or hrp
    if not origem then return end
    local posInicio = origem.Position
    if origem == hrp then posInicio = posInicio + Vector3.new(0, 1.5, 0) end
    local dir = Vector3.new(1, 0, 0)
    if head then dir = head.CFrame.LookVector
    elseif hrp then dir = hrp.CFrame.LookVector end
    dir = Vector3.new(dir.X, 0, dir.Z)
    if dir.Magnitude < 0.001 then dir = Vector3.new(1, 0, 0) else dir = dir.Unit end
    local posFim = posInicio + dir * 100
    criarLinhaVisao()
    local meio = (posInicio + posFim) / 2
    local dist = (posInicio - posFim).Magnitude
    if dist < 0.1 then dist = 0.1 end
    linhaVisao.Size = Vector3.new(1.0, 1.0, dist)
    linhaVisao.CFrame = CFrame.lookAt(meio, posFim)
end

local function criarLinhaChute()
    if not linhaChute or not linhaChute.Parent then
        if linhaChute then linhaChute:Destroy() end
        linhaChute = Instance.new("Part")
        linhaChute.Name = "LinhaChute"; linhaChute.Anchored = true; linhaChute.CanCollide = false
        linhaChute.Transparency = 0.2; linhaChute.Color = Color3.fromRGB(255, 255, 0)
        linhaChute.Material = Enum.Material.Neon; linhaChute.Size = Vector3.new(0.5, 0.5, 25)
        linhaChute.Parent = workspace
    end
    return linhaChute
end

local function criarCirculoChute()
    if not circuloChute or not circuloChute.Parent then
        if circuloChute then circuloChute:Destroy() end
        circuloChute = Instance.new("Part")
        circuloChute.Name = "CirculoChute"; circuloChute.Anchored = true; circuloChute.CanCollide = false
        circuloChute.Transparency = 0.2; circuloChute.Color = Color3.fromRGB(255, 255, 0)
        circuloChute.Material = Enum.Material.Neon; circuloChute.Shape = Enum.PartType.Cylinder
        circuloChute.Size = Vector3.new(4, 0.1, 4); circuloChute.Parent = workspace
    end
    return circuloChute
end

local function atualizarAdvinhaChute()
    if not advinhaChuteEnabled then
        if linhaChute then linhaChute:Destroy(); linhaChute = nil end
        if circuloChute then circuloChute:Destroy(); circuloChute = nil end
        return
    end
    if not bola or not bolaValida then
        if linhaChute then linhaChute:Destroy(); linhaChute = nil end
        if circuloChute then circuloChute:Destroy(); circuloChute = nil end
        return
    end
    local vel = bola.AssemblyLinearVelocity
    if vel.Magnitude < 0.5 then
        if linhaChute then linhaChute:Destroy(); linhaChute = nil end
        if circuloChute then circuloChute:Destroy(); circuloChute = nil end
        return
    end
    local gravidade = Vector3.new(0, -50, 0)
    local pos = bola.Position; local v = vel
    local dt = 0.02; local maxPassos = 500; local chaoY = 0
    local pontos = {pos}
    for i = 1, maxPassos do
        v = v + gravidade * dt; pos = pos + v * dt
        table.insert(pontos, pos)
        if pos.Y <= chaoY then break end
    end
    if #pontos < 2 then return end
    local posFinal = pontos[#pontos]
    posFinal = Vector3.new(posFinal.X, chaoY, posFinal.Z)
    local inicio = pontos[1]; local fim = pontos[#pontos]
    local meio = (inicio + fim) / 2
    local dist = (inicio - fim).Magnitude
    criarLinhaChute()
    if dist > 0.1 then
        linhaChute.Size = Vector3.new(0.5, 0.5, dist)
        linhaChute.CFrame = CFrame.lookAt(meio, fim)
        linhaChute.Transparency = 0.2
    else linhaChute.Transparency = 1 end
    if v.Y < -0.5 then
        criarCirculoChute()
        circuloChute.Position = posFinal
        local tam = math.clamp(vel.Magnitude / 8, 3, 8)
        circuloChute.Size = Vector3.new(tam, 0.1, tam)
        circuloChute.Transparency = 0.2
    else
        if circuloChute then circuloChute:Destroy(); circuloChute = nil end
    end
end

-- Chat
local function onChatMessage(msg)
    local lower = msg:lower()
    if lower:match("^força da bola") then
        local num = msg:match("^força da bola (%d+)")
        if num then
            local val = tonumber(num)
            if val and val > 0 and val <= 200 then
                forcaBolaVelocidade = val; Config.forcaBolaVelocidade = val; SaveConfig()
                player:Chat("Força da bola ajustada para " .. val)
            else player:Chat("Use um número entre 1 e 200.") end
        else player:Chat("Use: Força da bola [número]") end
    end
end

local function setupChatCommand()
    local chatService = ReplicatedStorage:FindFirstChild("DefaultChatSystemChatEvents")
    if chatService then
        local onMessage = chatService:FindFirstChild("OnMessage")
        if onMessage then onMessage.OnClientEvent:Connect(onChatMessage) end
    end
end
setupChatCommand()

-- Aimbot
local function ativarAimbotPersonagem(estado)
    aimbotPersonagem = estado
    Config.aimbotPersonagem = estado
    SaveConfig()
    if not estado and gyro then gyro:Destroy(); gyro = nil end
end

local function atualizarAimbotPersonagem()
    if not aimbotPersonagem then
        if gyro then gyro:Destroy(); gyro = nil end
        return
    end
    if not shouldAimbotWork() then
        if gyro then gyro:Destroy(); gyro = nil end
        return
    end
    if not hitbox or not bolaValida then
        if gyro then gyro:Destroy(); gyro = nil end
        return
    end
    local char = player.Character
    if not char then return end
    local hrp = char:FindFirstChild("HumanoidRootPart")
    if not hrp then return end
    if not gyro then
        gyro = Instance.new("BodyGyro")
        gyro.Parent = hrp; gyro.D = 1000; gyro.P = 10000
        gyro.MaxTorque = Vector3.new(8000, 8000, 8000)
    end
    local posBola = hitbox.Position
    local posJogador = hrp.Position
    local direcao = (Vector3.new(posBola.X, posJogador.Y, posBola.Z) - posJogador).Unit
    gyro.CFrame = CFrame.lookAt(posJogador, posJogador + direcao)
end

local function atualizarAimbotCamera()
    if isControlActive then return end
    if not aimbotCamera then return end
    if not shouldAimbotWork() then return end
    if not hitbox or not bolaValida then return end
    local char = player.Character
    if not char then return end
    local hrp = char:FindFirstChild("HumanoidRootPart")
    if not hrp then return end
    local posBola = hitbox.Position
    local posJogador = hrp.Position
    local direction = (Vector3.new(posBola.X, posJogador.Y, posBola.Z) - posJogador).Unit
    local camPos = hrp.Position - direction * 10 + Vector3.new(0, 5, 0)
    camera.CFrame = CFrame.new(camPos, hrp.Position)
end

local function atualizarESP()
    if euTenhoBola() or not mostrarESP or not hitbox then
        if espSphere then espSphere:Destroy(); espSphere = nil end
        return
    end
    if not espSphere then
        espSphere = Instance.new("SelectionSphere")
        espSphere.Name = "ReachESP"; espSphere.Adornee = hitbox; espSphere.Parent = hitbox
        espSphere.Color3 = Color3.fromRGB(0, 255, 0); espSphere.Transparency = 0.6
    end
    local scale = reachBolaDistancia / 2.5
    espSphere.Scale = Vector3.new(scale, scale, scale)
end

-- Botão Curvas
local function atualizarBotaoCurvas()
    if not curvasBotao then return end
    if curvasModo == 1 then
        curvasBotao.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
        curvasBotao.Text = "Curva: Direita ☛"
    elseif curvasModo == 2 then
        curvasBotao.BackgroundColor3 = Color3.fromRGB(0, 0, 255)
        curvasBotao.Text = "Curva: Esquerda ☚"
    elseif curvasModo == 3 then
        curvasBotao.BackgroundColor3 = Color3.fromRGB(0, 255, 0)
        curvasBotao.Text = "Curva: Aleatório 🥴"
    end
end

local function toggleCurvasModo()
    curvasModo = curvasModo + 1
    if curvasModo > 3 then curvasModo = 1 end
    atualizarBotaoCurvas()
end

local function criarBotaoCurvas()
    if curvasBotaoGUI then return end
    local screenGui = Instance.new("ScreenGui")
    screenGui.Name = "CurvasGui"; screenGui.ResetOnSpawn = false
    safeParent(screenGui)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0, 200, 0, 45); btn.Position = UDim2.new(0.5, -100, 0.3, 0)
    btn.BackgroundColor3 = Color3.fromRGB(255, 0, 0); btn.BackgroundTransparency = 0
    btn.Text = "Curva: Direita ☛"; btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.Font = Enum.Font.GothamBold; btn.TextSize = 16; btn.Active = true
    btn.Draggable = true; btn.Parent = screenGui
    local uiCorner = Instance.new("UICorner"); uiCorner.CornerRadius = UDim.new(0, 8); uiCorner.Parent = btn
    btn.MouseButton1Click:Connect(toggleCurvasModo)
    curvasBotao = btn; curvasBotaoGUI = screenGui
    atualizarBotaoCurvas()
end

local function removerBotaoCurvas()
    if curvasBotaoGUI then curvasBotaoGUI:Destroy(); curvasBotaoGUI = nil; curvasBotao = nil end
end

-- Anti-Tackle
task.spawn(function()
    while true do
        task.wait(0.05)
        if reachPersonagemAtivo then aplicarReachPersonagem() end
    end
end)

local tackleAnimIds = {"rasteira", "tackle", "slide", "17996440171"}
local tackleCooldown = 0

local function isTackleAnim(character)
    local humanoid = character:FindFirstChild("Humanoid")
    if not humanoid then return false end
    local animator = humanoid:FindFirstChild("Animator") or humanoid
    for _, track in ipairs(animator:GetPlayingAnimationTracks()) do
        if track.Animation then
            local id = tostring(track.Animation.AnimationId)
            for _, pattern in ipairs(tackleAnimIds) do
                if string.find(id:lower(), pattern) then return true end
            end
        end
    end
    return false
end

local function antiTackle()
    if tick() - tackleCooldown < 0.5 then return end
    local char = player.Character
    if not char then return end
    local myHrp = char:FindFirstChild("HumanoidRootPart")
    if not myHrp then return end
    for _, plr in ipairs(Players:GetPlayers()) do
        if plr ~= player and plr.Character then
            local enemyHrp = plr.Character:FindFirstChild("HumanoidRootPart")
            if enemyHrp then
                local dist = (enemyHrp.Position - myHrp.Position).Magnitude
                if dist < 8 and isTackleAnim(plr.Character) then
                    local warn = Instance.new("TextLabel")
                    warn.Size = UDim2.new(0, 200, 0, 30)
                    warn.Position = UDim2.new(0.5, -100, 0.7, 0)
                    warn.BackgroundTransparency = 1
                    warn.TextColor3 = Color3.fromRGB(255, 0, 0)
                    warn.TextStrokeTransparency = 0
                    warn.Text = "⚠️ RASTEIRA DETECTADA!"
                    warn.Font = Enum.Font.GothamBold
                    warn.TextSize = 18
                    warn.Parent = player.PlayerGui
                    game:GetService("Debris"):AddItem(warn, 0.8)
                    tackleCooldown = tick()
                    break
                end
            end
        end
    end
end

task.spawn(function()
    while true do
        task.wait(0.1)
        antiTackle()
    end
end)

-- Performance
local statsLabel = nil
local statsGui = nil

local function criarStatsGui()
    if statsGui then return end
    statsGui = Instance.new("ScreenGui")
    statsGui.Name = "StatsGui"; statsGui.ResetOnSpawn = false
    safeParent(statsGui)
    statsLabel = Instance.new("TextLabel")
    statsLabel.Size = UDim2.new(0, 250, 0, 60)
    statsLabel.Position = UDim2.new(1, -260, 1, -70)
    statsLabel.BackgroundTransparency = 1
    statsLabel.TextColor3 = Color3.fromRGB(255, 0, 0)
    statsLabel.Font = Enum.Font.GothamBold
    statsLabel.TextSize = 14
    statsLabel.TextXAlignment = Enum.TextXAlignment.Right
    statsLabel.TextYAlignment = Enum.TextYAlignment.Bottom
    statsLabel.Text = "FPS: --\nPING: --"
    statsLabel.Parent = statsGui
end

local function destruirStatsGui()
    if statsGui then statsGui:Destroy(); statsGui = nil; statsLabel = nil end
end

local function atualizarStats()
    if not statsLabel then return end
    local fps = math.floor(1 / RunService.RenderStepped:Wait())
    local ping = 0
    pcall(function()
        ping = math.floor(Stats.Network.ServerStatsItem["Data Ping"]:GetValue())
    end)
    statsLabel.Text = string.format("FPS: %d\nPING: %d ms", fps, ping)
end

local fpsOriginalSettings = nil
local fpsModeParticles = {}

local function salvarSettingsOriginais()
    if fpsOriginalSettings then return end
    fpsOriginalSettings = {
        GlobalShadows = Lighting.GlobalShadows,
        FogEnd = Lighting.FogEnd,
        Brightness = Lighting.Brightness,
        Ambient = Lighting.Ambient,
        OutdoorAmbient = Lighting.OutdoorAmbient,
    }
end

local function restaurarSettings()
    if not fpsOriginalSettings then return end
    Lighting.GlobalShadows = fpsOriginalSettings.GlobalShadows
    Lighting.FogEnd = fpsOriginalSettings.FogEnd
    Lighting.Brightness = fpsOriginalSettings.Brightness
    Lighting.Ambient = fpsOriginalSettings.Ambient
    Lighting.OutdoorAmbient = fpsOriginalSettings.OutdoorAmbient
    for _, obj in ipairs(fpsModeParticles) do
        if obj and obj.Parent then obj.Enabled = true end
    end
    fpsModeParticles = {}
end

local function aplicarFpsMode(level)
    if level == 0 then restaurarSettings(); return end
    salvarSettingsOriginais()
    if level >= 1 then
        Lighting.GlobalShadows = false
        Lighting.FogEnd = 100000
        pcall(function() sethiddenproperty(Lighting, "Technology", 2) end)
        pcall(function() Lighting.Technology = Enum.Technology.Compatibility end)
    end
    if level >= 2 then
        for _, obj in ipairs(workspace:GetDescendants()) do
            if obj:IsA("ParticleEmitter") or obj:IsA("Trail") or obj:IsA("Smoke")
                or obj:IsA("Fire") or obj:IsA("Sparkles") or obj:IsA("Explosion")
                or obj:IsA("Beam") or obj:IsA("PointLight") or obj:IsA("SpotLight")
                or obj:IsA("SurfaceLight") then
                if obj.Enabled then
                    obj.Enabled = false
                    table.insert(fpsModeParticles, obj)
                end
            end
        end
        Lighting.Brightness = 0
        Lighting.Ambient = Color3.fromRGB(128, 128, 128)
        Lighting.OutdoorAmbient = Color3.fromRGB(128, 128, 128)
    end
    if level >= 3 then
        pcall(function() settings().Rendering.QualityLevel = Enum.QualityLevel.Level01 end)
        pcall(function()
            for _, obj in ipairs(Lighting:GetChildren()) do
                if obj:IsA("Atmosphere") or obj:IsA("BloomEffect") or obj:IsA("BlurEffect")
                    or obj:IsA("ColorCorrectionEffect") or obj:IsA("SunRaysEffect")
                    or obj:IsA("DepthOfFieldEffect") then
                    obj.Enabled = false
                    table.insert(fpsModeParticles, obj)
                end
            end
        end)
    end
end

local function aplicarPingMenos(estado)
    Config.pingMenos = estado
    SaveConfig()
    if estado then
        pcall(function() settings().Rendering.QualityLevel = Enum.QualityLevel.Level01 end)
        pcall(function() sethiddenproperty(Lighting, "Technology", 2) end)
        pcall(function()
            for _, obj in ipairs(workspace:GetDescendants()) do
                if obj:IsA("ParticleEmitter") then obj.Enabled = false end
            end
        end)
    else
        pcall(function() settings().Rendering.QualityLevel = Enum.QualityLevel.Automatic end)
    end
end

-- Loop principal
RunService.Heartbeat:Connect(function()
    aplicarReachBola()
    atualizarAimbotPersonagem()
    atualizarAimbotCamera()
    atualizarESP()
    atualizarVisaoBola()
    atualizarAdvinhaChute()
    aplicarForcaBola()
    aplicarCurvas()
    aplicarDrible()
end)

task.spawn(function()
    while true do
        atualizarStats()
        task.wait(0.5)
    end
end)

-- =============================================
-- UI (com prints de debug)
-- =============================================
print("[Blue Lock] Criando UI...")

local MainTab = Window:Tab({ Title = "Blue Lock", Icon = "rbxassetid://135710581431595" })
print("[Blue Lock] Tab principal criada")

-- ===== Controle de Bola =====
MainTab:Section({ Title = "Controle de Bola" })

MainTab:Input({
    Title = "Velocidade da Bola",
    Desc = "Força com que a bola é empurrada no controle",
    Value = tostring(Config.velocidadeBola),
    Placeholder = "Digite a velocidade...",
    Callback = function(txt)
        local val = tonumber(txt)
        if val then velocidadeBola = val; Config.velocidadeBola = val; SaveConfig() end
    end
})
print("[Blue Lock] Input Velocidade OK")

MainTab:Toggle({
    Title = "Botão Flutuante",
    Desc = "Cria um botão arrastável para controlar a bola",
    Value = Config.botaoFlutuante,
    Callback = function(v)
        Config.botaoFlutuante = v; SaveConfig()
        if v then criarBotaoFlutuante() else removerBotaoFlutuante() end
    end
})
print("[Blue Lock] Toggle Botão Flutuante OK")

-- ===== Aimbot =====
MainTab:Section({ Title = "Aimbot" })

MainTab:Toggle({
    Title = "Aimbot Personagem",
    Desc = "Corpo vira para a bola",
    Value = Config.aimbotPersonagem,
    Callback = function(v) ativarAimbotPersonagem(v) end
})

MainTab:Toggle({
    Title = "Aimbot Câmera",
    Desc = "Câmera atrás do personagem olhando para a bola",
    Value = Config.aimbotCamera,
    Callback = function(v)
        aimbotCamera = v; Config.aimbotCamera = v; SaveConfig()
        if cameraToggleButton then
            if v then
                cameraToggleButton.BackgroundColor3 = Color3.fromRGB(40, 167, 69)
                cameraToggleButton.Text = "Aimbot Cam [ON]"
                cameraButtonActive = true
            else
                cameraToggleButton.BackgroundColor3 = Color3.fromRGB(220, 53, 69)
                cameraToggleButton.Text = "Aimbot Cam [OFF]"
                cameraButtonActive = false
            end
        end
    end
})

MainTab:Toggle({
    Title = "Botão Voador (Câmera)",
    Desc = "Botão flutuante para ligar/desligar a câmera",
    Value = Config.botaoCamera,
    Callback = function(v)
        Config.botaoCamera = v; SaveConfig()
        if v then criarBotaoCameraFlutuante() else removerBotaoCameraFlutuante() end
    end
})

MainTab:Slider({
    Title = "Tempo do Aimbot",
    Desc = "Atraso após perder a bola (0-10)",
    Step = 1,
    Value = { Min = 0, Max = 10, Default = Config.aimbotDelayValue },
    Callback = function(v)
        aimbotDelayValue = aimbotDelayValues[v + 1] or 0
        Config.aimbotDelayValue = v; SaveConfig()
    end
})
print("[Blue Lock] Aimbot widgets OK")

-- ===== Visão de Jogo =====
MainTab:Section({ Title = "Visão de Jogo" })

MainTab:Toggle({
    Title = "Visão da bola",
    Desc = "Linha verde saindo da cabeça do inimigo com a bola",
    Value = Config.visaoBola,
    Callback = function(v)
        visaoBolaEnabled = v; Config.visaoBola = v; SaveConfig()
        if not v and linhaVisao then linhaVisao:Destroy(); linhaVisao = nil end
    end
})

MainTab:Toggle({
    Title = "Advinha Chute",
    Desc = "Mostra a trajetória da bola e o ponto de queda",
    Value = Config.advinhaChute,
    Callback = function(v)
        advinhaChuteEnabled = v; Config.advinhaChute = v; SaveConfig()
        if not v then
            if linhaChute then linhaChute:Destroy(); linhaChute = nil end
            if circuloChute then circuloChute:Destroy(); circuloChute = nil end
        end
    end
})

-- ===== REACH =====
MainTab:Section({ Title = "REACH" })

MainTab:Toggle({
    Title = "REACH Personagem",
    Desc = "Coleta a bola de longe",
    Value = Config.reachPersonagem,
    Callback = function(v)
        reachPersonagemAtivo = v; Config.reachPersonagem = v; SaveConfig()
    end
})

MainTab:Slider({
    Title = "Alcance do REACH",
    Desc = "Raio de detecção do REACH (1-30)",
    Step = 1,
    Value = { Min = 1, Max = 30, Default = Config.reachPersonagemTamanho },
    Callback = function(v)
        reachPersonagemTamanho = v; Config.reachPersonagemTamanho = v; SaveConfig()
    end
})

MainTab:Toggle({
    Title = "REACH da Bola",
    Desc = "Aumenta a hitbox da bola (invisível)",
    Value = Config.reachBola,
    Callback = function(v) reachBolaAtivo = v; Config.reachBola = v; SaveConfig() end
})

MainTab:Slider({
    Title = "Tamanho da Hitbox da Bola",
    Desc = "Tamanho da hitbox da bola (1-30)",
    Step = 1,
    Value = { Min = 1, Max = 30, Default = Config.reachBolaTamanho },
    Callback = function(v)
        reachBolaDistancia = v; Config.reachBolaTamanho = v; SaveConfig()
        if espSphere then espSphere.Scale = Vector3.new(v/2.5, v/2.5, v/2.5) end
    end
})

MainTab:Toggle({
    Title = "Mostrar Área ESP",
    Desc = "Círculo verde ao redor da hitbox da bola",
    Value = Config.mostrarESP,
    Callback = function(v)
        mostrarESP = v; Config.mostrarESP = v; SaveConfig()
        if not v and espSphere then espSphere:Destroy(); espSphere = nil end
    end
})
print("[Blue Lock] REACH widgets OK")

-- ===== Extras =====
MainTab:Section({ Title = "Extras" })

MainTab:Toggle({
    Title = "Força da Bola",
    Desc = "Chute sempre com potência máxima",
    Value = Config.forcaBola,
    Callback = function(v) forcaBolaEnabled = v; Config.forcaBola = v; SaveConfig() end
})

MainTab:Slider({
    Title = "Força Máxima",
    Desc = "Velocidade do chute (10-200)",
    Step = 1,
    Value = { Min = 10, Max = 200, Default = Config.forcaBolaVelocidade },
    Callback = function(v) forcaBolaVelocidade = v; Config.forcaBolaVelocidade = v; SaveConfig() end
})

MainTab:Button({
    Title = "Stamina Infinita",
    Desc = "Stamina sempre no máximo",
    Callback = function()
        task.spawn(function()
            while true do
                if player:FindFirstChild("PlayerStats") and player.PlayerStats:FindFirstChild("Stamina") then
                    player.PlayerStats.Stamina.Value = 100
                end
                task.wait()
            end
        end)
    end
})

-- ✅ DROPDOWN - sintaxe correta da WindUI ukinabr
local dropdownTime = MainTab:Dropdown({
    Title = "Time",
    Desc = "Selecione para fazer gol",
    Values = {"Home", "Away"},
    Value = "Home",
    Callback = function(opt)
        selectedTeam = opt
        Config.timeSelecionado = opt
        SaveConfig()
        print("[Blue Lock] Dropdown Time =", opt)
    end
})
print("[Blue Lock] Dropdown Time OK - valor padrão: Home")

MainTab:Button({
    Title = "Fazer Gol",
    Desc = "Marca um gol automaticamente",
    Callback = function()
        if not selectedTeam then
            print("[Blue Lock] Time não selecionado!")
            return
        end
        print("[Blue Lock] Tentando fazer gol no time: " .. selectedTeam)
        local char = player.Character
        if not char then return end
        local hrp = char:FindFirstChild("HumanoidRootPart")
        if not hrp then return end
        local posOriginal = hrp.Position
        local posGol = selectedTeam == "Home" and Vector3.new(-234, 11, -47) or Vector3.new(311, 11, -50)
        hrp.CFrame = CFrame.new(posGol)
        task.wait(0.2)
        local args = { [1] = 110, [4] = posGol }
        local ballService = ReplicatedStorage:FindFirstChild("Packages")
        if ballService then
            ballService = ballService:FindFirstChild("Knit")
            if ballService then
                ballService = ballService:FindFirstChild("Services")
                if ballService then
                    ballService = ballService:FindFirstChild("BallService")
                    if ballService then
                        local shoot = ballService:FindFirstChild("RE")
                        if shoot then shoot:FireServer(unpack(args)) end
                    end
                end
            end
        end
        task.wait(0.2)
        hrp.CFrame = CFrame.new(posOriginal)
    end
})

-- ===== Curvas =====
MainTab:Section({ Title = "Curvas" })

MainTab:Toggle({
    Title = "Bola com curvas",
    Desc = "Ativa curvas na bola",
    Value = Config.curvasAtivo,
    Callback = function(v)
        curvasEnabled = v; Config.curvasAtivo = v; SaveConfig()
        if v then criarBotaoCurvas() else removerBotaoCurvas() end
    end
})

MainTab:Slider({
    Title = "Intensidade da Curva",
    Desc = "1-5 fraco | 6-8 médio | 9 forte | 10 muito forte",
    Step = 1,
    Value = { Min = 1, Max = 10, Default = Config.curvasIntensidade },
    Callback = function(v) curvasIntensidade = v; Config.curvasIntensidade = v; SaveConfig() end
})

-- ===== Drible =====
MainTab:Section({ Title = "Drible" })

MainTab:Toggle({
    Title = "Drible",
    Desc = "Círculo rosa de detecção (raio 70)",
    Value = Config.dribleAtivo,
    Callback = function(v)
        dribleEnabled = v; Config.dribleAtivo = v; SaveConfig()
        if v then setupDrible()
        else if dribleSphere then dribleSphere.Transparency = 1 end end
    end
})
print("[Blue Lock] Extras + Curvas + Drible widgets OK")

-- =============================================
-- ABA DESEMPENHO
-- =============================================
local PerfTab = Window:Tab({ Title = "Desempenho", Icon = "rbxassetid://135710581431595" })

PerfTab:Section({ Title = "Performance" })

PerfTab:Toggle({
    Title = "Mostrar Ping e FPS",
    Desc = "Exibe FPS e Ping no canto inferior direito (vermelho)",
    Value = Config.statsToggle,
    Callback = function(v)
        Config.statsToggle = v; SaveConfig()
        if v then criarStatsGui() else destruirStatsGui() end
    end
})

PerfTab:Toggle({
    Title = "Modo FPS++",
    Desc = "Remove sombras, efeitos, partículas e vibrações",
    Value = Config.fpsModeAtivo,
    Callback = function(v)
        Config.fpsModeAtivo = v; SaveConfig()
        if v then aplicarFpsMode(Config.fpsModeLevel) else aplicarFpsMode(0) end
    end
})

PerfTab:Slider({
    Title = "Nível FPS++",
    Desc = "0 = Desligado | 1 = Sombras | 2 = Partículas | 3 = Tudo",
    Step = 1,
    Value = { Min = 0, Max = 3, Default = Config.fpsModeLevel },
    Callback = function(v)
        Config.fpsModeLevel = v; SaveConfig()
        if Config.fpsModeAtivo then aplicarFpsMode(v) end
    end
})

PerfTab:Toggle({
    Title = "Ping -10",
    Desc = "Reduz qualidade gráfica para diminuir latência visual",
    Value = Config.pingMenos,
    Callback = function(v) aplicarPingMenos(v) end
})

-- =============================================
-- SALVAR
-- =============================================
MainTab:Section({ Title = "Salvar Configurações" })
MainTab:Button({
    Title = "💾 Salvar Agora",
    Desc = "Salva todas as configurações manualmente",
    Callback = function()
        SaveConfig()
        StarterGui:SetCore("SendNotification", {
            Title = "Blue Lock",
            Text = "Configurações salvas!",
            Duration = 2
        })
    end
})

-- Aplica estados salvos
if Config.statsToggle then criarStatsGui() end
if Config.fpsModeAtivo and Config.fpsModeLevel > 0 then aplicarFpsMode(Config.fpsModeLevel) end
if Config.pingMenos then aplicarPingMenos(true) end

print("[Blue Lock] Script carregado com sucesso!")

pcall(function()
    StarterGui:SetCore("SendNotification",{
        Title = "Blue Lock Rivals",
        Text = "Carregado!",
        Duration = 3
    })
end)
