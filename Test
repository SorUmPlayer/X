--[[
    Volleyball Legends – Interface Nova
]]

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local Workspace = game:GetService("Workspace")
local CoreGui = game:GetService("CoreGui")
local HttpService = game:GetService("HttpService")
local StarterGui = game:GetService("StarterGui")

local player = Players.LocalPlayer

-- =============================================
-- CONFIGURAÇÕES PERSISTENTES
-- =============================================
local CONFIG_FILE = "VolleyballConfig.json"

local function LoadConfig()
    if not isfile then return {} end
    if isfile(CONFIG_FILE) then
        local success, data = pcall(function()
            return HttpService:JSONDecode(readfile(CONFIG_FILE))
        end)
        if success and type(data) == "table" then
            return data
        end
    end
    return {}
end

local function SaveConfigToFile(configTable)
    if not writefile then return end
    local success, json = pcall(function()
        return HttpService:JSONEncode(configTable)
    end)
    if success then
        pcall(function() writefile(CONFIG_FILE, json) end)
    end
end

local Config = {
    Hitbox = false,
    Tamanho = 15
}
local saved = LoadConfig()
for k, v in pairs(saved) do
    Config[k] = v
end

local function SaveConfig()
    SaveConfigToFile(Config)
end

-- =============================================
-- CARREGA WINDUI
-- =============================================
local WindUI = loadstring(game:HttpGet("https://raw.githubusercontent.com/ukinabr/WindUI/refs/heads/main/dist/main.lua"))()

local Window = WindUI:CreateWindow({
    Title = "🏐 Volleyball Legends",
    Icon = "rbxassetid://135710581431595",
    Author = "Sor_Gostoso",
    Folder = "Volleyball",
    Size = UDim2.fromOffset(400, 300),
    Transparent = false,
    Theme = "Dark",
    Resizable = true,
    SideBarWidth = 200,
    BackgroundImageTransparency = 0.42,
    HideSearchBar = true,
    ScrollBarEnabled = false,
})

-- =============================================
-- CÓDIGO FUNCIONAL (100% ORIGINAL)
-- =============================================
local vbHitboxEnabled = false
local vbHitboxSize = 15
local vbOriginalProps = {}

local function restoreVbOriginals()
    for part, props in pairs(vbOriginalProps) do
        if part and part.Parent then
            part.Size = props.Size
            part.Transparency = props.Transparency
            part.CanCollide = props.CanCollide
            part.CanTouch = props.CanTouch
        end
    end
    table.clear(vbOriginalProps)
end

local function getVbBallParts()
    local ballParts = {}
    for _, item in ipairs(Workspace:GetChildren()) do
        if string.sub(item.Name, 1, 12) == "CLIENT_BALL_" then
            if item:IsA("BasePart") then
                table.insert(ballParts, item)
            else
                for _, child in ipairs(item:GetDescendants()) do
                    if child:IsA("BasePart") then
                        table.insert(ballParts, child)
                    end
                end
            end
        end
    end
    return ballParts
end

RunService.Heartbeat:Connect(function()
    if not vbHitboxEnabled then return end

    local char = player.Character
    if not char then return end
    local hrp = char:FindFirstChild("HumanoidRootPart")
    if not hrp then return end

    local bodyParts = {
        char:FindFirstChild("Right Foot") or char:FindFirstChild("RightLeg") or char:FindFirstChild("Right Lower Leg"),
        char:FindFirstChild("Left Foot") or char:FindFirstChild("LeftLeg") or char:FindFirstChild("Left Lower Leg"),
        hrp
    }

    local balls = getVbBallParts()
    for _, part in ipairs(balls) do
        if not vbOriginalProps[part] then
            vbOriginalProps[part] = {
                Size = part.Size,
                Transparency = part.Transparency,
                CanCollide = part.CanCollide,
                CanTouch = part.CanTouch
            }
        end

        part.Size = Vector3.new(vbHitboxSize, vbHitboxSize, vbHitboxSize)
        part.Transparency = 0.5
        part.CanCollide = false
        part.CanTouch = true
        if pcall(function() return part.CanQuery end) then
            part.CanQuery = true
        end

        local distance = (hrp.Position - part.Position).Magnitude
        if distance <= (vbHitboxSize / 2) + 3 then
            for _, bPart in ipairs(bodyParts) do
                if bPart and firetouchinterest then
                    firetouchinterest(bPart, part, 0)
                    firetouchinterest(bPart, part, 1)
                end
            end
        end
    end
end)

-- =============================================
-- INTERFACE
-- =============================================
local TabMain = Window:Tab({ Title = "Volleyball", Icon = "rbxassetid://135710581431595" })
local secVb = TabMain:Section({ Title = "Volleyball Legends" })

secVb:Toggle({
    Title = "Hitbox",
    Value = Config.Hitbox,
    Callback = function(value)
        Config.Hitbox = value
        SaveConfig()
        vbHitboxEnabled = value
        if not value then
            restoreVbOriginals()
        end
    end
})
secVb:Slider({
    Title = "Tamanho",
    Value = Config.Tamanho,
    Min = 1,
    Max = 50,
    Step = 1,
    Callback = function(value)
        Config.Tamanho = value
        SaveConfig()
        vbHitboxSize = value
    end
})

-- Aba Config
local TabConfig = Window:Tab({ Title = "Config", Icon = "rbxassetid://135710581431595" })
TabConfig:Section({ Title = "Salvamento Automático" })
TabConfig:Paragraph({
    Title = "Status",
    Desc = "Todas as configurações são salvas automaticamente."
})
TabConfig:Button({
    Title = "Resetar configurações",
    Callback = function()
        Config.Hitbox = false
        Config.Tamanho = 15
        SaveConfig()
        StarterGui:SetCore("SendNotification", {
            Title = "Config",
            Text = "Configurações resetadas! Recarregue o script.",
            Duration = 3
        })
    end
})

StarterGui:SetCore("SendNotification", {
    Title = "Volleyball Legends",
    Text = "Carregado com sucesso!",
    Duration = 3
})
