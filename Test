--[[
    Azure Lock Hub – Aimbot, Visão, Drible (ByteNet) – Linha Amarela final
]]

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local Workspace = game:GetService("Workspace")
local CoreGui = game:GetService("CoreGui")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local ByteNet = ReplicatedStorage:FindFirstChild("ByteNetReliable")

local player = Players.LocalPlayer
local camera = Workspace.CurrentCamera

-- =====================================================
--  FUNÇÃO SEGURA DE PARENT
-- =====================================================
local function safeParent(gui)
    local ok = pcall(function()
        if gethui then
            gui.Parent = gethui()
        elseif syn and syn.protect_gui then
            syn.protect_gui(gui)
            gui.Parent = CoreGui
        else
            gui.Parent = CoreGui
        end
    end)
    if not ok then
        gui.Parent = player:WaitForChild("PlayerGui")
    end
end

-- =====================================================
--  DETECÇÃO DA BOLA (DINÂMICA)
-- =====================================================
local function getBall()
    -- 1. Bola solta (workspace.Terrain.Ball)
    local terrain = Workspace:FindFirstChild("Terrain")
    if terrain then
        local ball = terrain:FindFirstChild("Ball")
        if ball and ball:IsA("BasePart") then return ball end
    end

    -- 2. Bola em posse (workspace.characters.*.Ball)
    local characters = Workspace:FindFirstChild("characters")
    if characters then
        for _, charModel in ipairs(characters:GetChildren()) do
            if charModel:IsA("Model") then
                local ball = charModel:FindFirstChild("Ball")
                if ball and ball:IsA("BasePart") then return ball end
            end
        end
    end

    -- 3. Fallback
    for _, obj in ipairs(Workspace:GetDescendants()) do
        if obj:IsA("BasePart") and obj.Name == "Ball" then
            return obj
        end
    end
    return nil
end

local function getHitbox()
    return getBall()
end

local function euTenhoBola()
    local characters = Workspace:FindFirstChild("characters")
    if characters then
        local myChar = characters:FindFirstChild(player.Name)
        if myChar then
            return myChar:FindFirstChild("Ball") ~= nil
        end
    end
    return false
end

-- =====================================================
--  VARIÁVEIS DE CONTROLE
-- =====================================================
local aimbotPersonagem = false
local aimbotCamera = false

-- Delay do Aimbot
local aimbotDelayValues = {0, 0.1, 0.2, 0.25, 0.40, 0.60, 0.70, 1.0, 2.0, 3.0, 5.0}
local aimbotDelayValue = 0
local aimbotBlocked = false
local aimbotBlockedUntil = 0

local visaoBolaEnabled = false
local advinhaChuteEnabled = false
local linhaVisao = nil
local linhaChute = nil
local circuloChute = nil

-- Drible
local dribleEnabled = false
local dribleSphere = nil
local dribleLastFired = 0
local dribleCooldown = 0.2
local dribleRadius = 15

-- Botão Voador da Câmera
local cameraButtonActive = false
local cameraScreenGui = nil
local cameraToggleButton = nil

-- =====================================================
--  LÓGICA DE BLOQUEIO DO AIMBOT (DELAY)
-- =====================================================
local function shouldAimbotWork()
    local tenho = euTenhoBola()
    if tenho then
        aimbotBlocked = true
        aimbotBlockedUntil = tick() + aimbotDelayValue
        return false
    end
    if aimbotBlocked then
        if tick() >= aimbotBlockedUntil then
            aimbotBlocked = false
            return true
        else
            return false
        end
    end
    return true
end

-- =====================================================
--  AIMBOT PERSONAGEM
-- =====================================================
local gyro = nil
local function ativarAimbotPersonagem(estado)
    aimbotPersonagem = estado
    if not estado and gyro then
        gyro:Destroy()
        gyro = nil
    end
end

local function atualizarAimbotPersonagem()
    if not aimbotPersonagem then
        if gyro then
            gyro:Destroy()
            gyro = nil
        end
        return
    end

    if not shouldAimbotWork() then
        if gyro then
            gyro:Destroy()
            gyro = nil
        end
        return
    end

    local hitbox = getHitbox()
    if not hitbox then
        if gyro then
            gyro:Destroy()
            gyro = nil
        end
        return
    end

    local char = player.Character
    if not char then return end
    local hrp = char:FindFirstChild("HumanoidRootPart")
    if not hrp then return end

    if not gyro then
        gyro = Instance.new("BodyGyro")
        gyro.Parent = hrp
        gyro.D = 1000
        gyro.P = 10000
        gyro.MaxTorque = Vector3.new(8000, 8000, 8000)
    end

    local posBola = hitbox.Position
    local posJogador = hrp.Position
    local direcao = (Vector3.new(posBola.X, posJogador.Y, posBola.Z) - posJogador).Unit
    gyro.CFrame = CFrame.lookAt(posJogador, posJogador + direcao)
end

-- =====================================================
--  AIMBOT CÂMERA
-- =====================================================
local function atualizarAimbotCamera()
    if not aimbotCamera then return end
    if not shouldAimbotWork() then return end

    local hitbox = getHitbox()
    if not hitbox then return end

    local char = player.Character
    if not char then return end
    local hrp = char:FindFirstChild("HumanoidRootPart")
    if not hrp then return end

    local posBola = hitbox.Position
    local posJogador = hrp.Position
    local direction = (Vector3.new(posBola.X, posJogador.Y, posBola.Z) - posJogador).Unit

    local camPos = hrp.Position - direction * 10 + Vector3.new(0, 5, 0)
    camera.CFrame = CFrame.new(camPos, hrp.Position)
end

-- =====================================================
--  VISÃO DA BOLA (LINHA VERDE)
-- =====================================================
local function getInimigoComBola()
    local characters = Workspace:FindFirstChild("characters")
    if not characters then return nil end

    for _, charModel in ipairs(characters:GetChildren()) do
        if charModel:IsA("Model") and charModel.Name ~= player.Name then
            if charModel:FindFirstChild("Ball") then
                return charModel
            end
        end
    end
    return nil
end

local function criarLinhaVisao()
    if not linhaVisao or not linhaVisao.Parent then
        if linhaVisao then linhaVisao:Destroy() end
        linhaVisao = Instance.new("Part")
        linhaVisao.Name = "LinhaVisao"
        linhaVisao.Anchored = true
        linhaVisao.CanCollide = false
        linhaVisao.Transparency = 0.15
        linhaVisao.Color = Color3.fromRGB(0, 255, 0)
        linhaVisao.Material = Enum.Material.Neon
        linhaVisao.Size = Vector3.new(0.8, 0.8, 100)
        linhaVisao.Parent = workspace
    end
    return linhaVisao
end

local function atualizarVisaoBola()
    if not visaoBolaEnabled then
        if linhaVisao then linhaVisao:Destroy() linhaVisao = nil end
        return
    end

    local inimigo = getInimigoComBola()
    if not inimigo then
        if linhaVisao then linhaVisao:Destroy() linhaVisao = nil end
        return
    end

    local head = inimigo:FindFirstChild("Head")
    local hrp = inimigo:FindFirstChild("HumanoidRootPart")
    local origem = head or hrp or inimigo.PrimaryPart
    if not origem then
        if linhaVisao then linhaVisao:Destroy() linhaVisao = nil end
        return
    end

    local posInicio = origem.Position
    if origem == hrp then
        posInicio = posInicio + Vector3.new(0, 1.5, 0)
    end

    local direcao = Vector3.new(1, 0, 0)
    if head then
        direcao = head.CFrame.LookVector
    elseif hrp then
        direcao = hrp.CFrame.LookVector
    end
    direcao = Vector3.new(direcao.X, 0, direcao.Z)
    if direcao.Magnitude < 0.001 then
        direcao = Vector3.new(1, 0, 0)
    else
        direcao = direcao.Unit
    end

    local posFim = posInicio + direcao * 100

    criarLinhaVisao()
    local meio = (posInicio + posFim) / 2
    local distancia = (posInicio - posFim).Magnitude
    if distancia < 0.1 then distancia = 0.1 end
    linhaVisao.Size = Vector3.new(0.8, 0.8, distancia)
    linhaVisao.CFrame = CFrame.lookAt(meio, posFim)
    linhaVisao.Transparency = 0.15
end

-- =====================================================
--  ADVINHA CHUTE (LINHA AMARELA – CORRIGIDA)
-- =====================================================
local function atualizarAdvinhaChute()
    if not advinhaChuteEnabled then
        if linhaChute then linhaChute:Destroy() linhaChute = nil end
        if circuloChute then circuloChute:Destroy() circuloChute = nil end
        return
    end

    local ball = getBall()
    if not ball then
        if linhaChute then linhaChute:Destroy() linhaChute = nil end
        if circuloChute then circuloChute:Destroy() circuloChute = nil end
        return
    end

    local vel = ball.AssemblyLinearVelocity
    local speed = vel.Magnitude

    -- Se a bola estiver parada, mantém a linha invisível mas não destrói (evita flicker)
    if speed < 0.5 then
        if linhaChute then
            linhaChute.Transparency = 1
        end
        if circuloChute then
            circuloChute.Transparency = 1
        end
        return
    end

    -- Simulação física (trajetória parabólica)
    local gravidade = Vector3.new(0, -55, 0)
    local pos = ball.Position
    local v = vel
    local dt = 0.03
    local maxPassos = 120
    local chaoY = 0

    local pontos = {}
    table.insert(pontos, pos)

    for i = 1, maxPassos do
        v = v + gravidade * dt
        pos = pos + v * dt
        table.insert(pontos, pos)
        if pos.Y <= chaoY then
            break
        end
    end

    if #pontos < 2 then
        if linhaChute then linhaChute:Destroy() linhaChute = nil end
        if circuloChute then circuloChute:Destroy() circuloChute = nil end
        return
    end

    local inicio = pontos[1]
    local fim = pontos[#pontos]
    fim = Vector3.new(fim.X, chaoY, fim.Z) -- forçar chão

    local meio = (inicio + fim) / 2
    local distancia = (inicio - fim).Magnitude

    -- Cria/atualiza linha amarela (única parte)
    if not linhaChute then
        linhaChute = Instance.new("Part")
        linhaChute.Name = "LinhaChute"
        linhaChute.Anchored = true
        linhaChute.CanCollide = false
        linhaChute.Transparency = 0.2
        linhaChute.Color = Color3.fromRGB(255, 255, 0)
        linhaChute.Material = Enum.Material.Neon
        linhaChute.Parent = workspace
    end

    -- Tamanho mínimo para garantir visibilidade
    local tamanho = math.max(distancia, 1)
    linhaChute.Size = Vector3.new(0.5, 0.5, tamanho)
    linhaChute.CFrame = CFrame.lookAt(meio, fim)
    linhaChute.Transparency = 0.2

    -- Círculo de impacto (se a bola estiver descendo)
    if v.Y < -0.5 then
        if not circuloChute then
            circuloChute = Instance.new("Part")
            circuloChute.Name = "CirculoChute"
            circuloChute.Anchored = true
            circuloChute.CanCollide = false
            circuloChute.Transparency = 0.2
            circuloChute.Color = Color3.fromRGB(255, 255, 0)
            circuloChute.Material = Enum.Material.Neon
            circuloChute.Shape = Enum.PartType.Cylinder
            circuloChute.Size = Vector3.new(4, 0.1, 4)
            circuloChute.Parent = workspace
        end
        circuloChute.Position = fim
        local tamanhoCirculo = math.clamp(speed / 8, 3, 8)
        circuloChute.Size = Vector3.new(tamanhoCirculo, 0.1, tamanhoCirculo)
        circuloChute.Transparency = 0.2
    else
        if circuloChute then
            circuloChute.Transparency = 1 -- esconde, não destrói
        end
    end
end

-- =====================================================
--  DRIBLE (APENAS DRIBLE)
-- =====================================================
local function dispararDrible()
    if not ByteNet then return end
    local args = {24, 1}
    local buffer = buffer.create(#args)
    for i, v in ipairs(args) do
        buffer.writeu8(buffer, i - 1, v)
    end
    ByteNet:FireServer(buffer, { {"dribble", false} })
end

local function setupDrible()
    if not dribleSphere then
        dribleSphere = Instance.new("Part")
        dribleSphere.Name = "DetectionSphereVisual"
        dribleSphere.Shape = Enum.PartType.Ball
        dribleSphere.Size = Vector3.new(dribleRadius * 2, dribleRadius * 2, dribleRadius * 2)
        dribleSphere.Material = Enum.Material.ForceField
        dribleSphere.Color = Color3.fromRGB(255, 0, 128)
        dribleSphere.Transparency = 0.6
        dribleSphere.CanCollide = false
        dribleSphere.Anchored = false
        dribleSphere.CastShadow = false
        dribleSphere.Parent = workspace
    end
end

local function isLookingAtLocal(targetHrp, localHrp)
    local directionToLocal = (localHrp.Position - targetHrp.Position).Unit
    local lookVector = targetHrp.CFrame.LookVector
    return lookVector:Dot(directionToLocal) >= 0.5
end

local function aplicarDrible()
    if not dribleEnabled then
        if dribleSphere then
            dribleSphere.Transparency = 1
        end
        return
    end

    local char = player.Character
    if not char then return end
    local hrp = char:FindFirstChild("HumanoidRootPart")
    if not hrp then return end

    if dribleSphere then
        dribleSphere.Position = hrp.Position
        dribleSphere.Transparency = 0.6
        dribleSphere.Parent = workspace
    end

    local now = tick()
    if now - dribleLastFired < dribleCooldown then return end

    local characters = Workspace:FindFirstChild("characters")
    if not characters then return end

    for _, charModel in ipairs(characters:GetChildren()) do
        if charModel:IsA("Model") and charModel ~= char then
            local targetHrp = charModel:FindFirstChild("HumanoidRootPart")
            if targetHrp then
                local dist = (targetHrp.Position - hrp.Position).Magnitude
                if dist <= dribleRadius then
                    if isLookingAtLocal(targetHrp, hrp) then
                        dribleLastFired = now
                        dispararDrible()
                        break
                    end
                end
            end
        end
    end
end

-- =====================================================
--  BOTÃO VOADOR DA CÂMERA
-- =====================================================
local function toggleCameraButton()
    cameraButtonActive = not cameraButtonActive
    if cameraButtonActive then
        if cameraToggleButton then
            cameraToggleButton.BackgroundColor3 = Color3.fromRGB(40, 167, 69)
            cameraToggleButton.Text = "Aimbot Cam [ON]"
        end
        aimbotCamera = true
    else
        if cameraToggleButton then
            cameraToggleButton.BackgroundColor3 = Color3.fromRGB(220, 53, 69)
            cameraToggleButton.Text = "Aimbot Cam [OFF]"
        end
        aimbotCamera = false
    end
end

local function criarBotaoCameraFlutuante()
    if cameraScreenGui then return end

    local screenGui = Instance.new("ScreenGui")
    screenGui.Name = "CameraAimbotGui"
    screenGui.ResetOnSpawn = false
    safeParent(screenGui)

    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0, 180, 0, 45)
    btn.Position = UDim2.new(0.5, -90, 0.15, 0)
    btn.BackgroundColor3 = Color3.fromRGB(220, 53, 69)
    btn.Text = "Aimbot Cam [OFF]"
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.Font = Enum.Font.GothamBold
    btn.TextSize = 16
    btn.Active = true
    btn.Draggable = true
    btn.Parent = screenGui

    local uiCorner = Instance.new("UICorner")
    uiCorner.CornerRadius = UDim.new(0, 8)
    uiCorner.Parent = btn

    btn.MouseButton1Click:Connect(toggleCameraButton)

    cameraToggleButton = btn
    cameraScreenGui = screenGui
end

local function removerBotaoCameraFlutuante()
    if cameraButtonActive then
        toggleCameraButton()
    end
    if cameraScreenGui then
        cameraScreenGui:Destroy()
        cameraScreenGui = nil
        cameraToggleButton = nil
    end
end

-- =====================================================
--  LOOP PRINCIPAL
-- =====================================================
RunService.Heartbeat:Connect(function()
    atualizarAimbotPersonagem()
    atualizarAimbotCamera()
    atualizarVisaoBola()
    atualizarAdvinhaChute()
    aplicarDrible()
end)

-- =====================================================
--  INTERFACE WINDUI (AZURE LOCK)
-- =====================================================
local WindUI
local loaded, _ = pcall(function()
    WindUI = loadstring(game:HttpGet("https://raw.githubusercontent.com/Footagesus/WindUI/main/dist/main.lua"))()
end)

if not loaded or not WindUI then
    WindUI = loadstring(game:HttpGet("https://github.com/Footagesus/WindUI/releases/latest/download/main.lua"))()
end

local Window = WindUI:CreateWindow({
    Title = "🌊 Azure Lock",
    Icon = "rbxassetid://10723343321",
    Author = "Sor_Gostoso",
    Folder = "AzureHub",
    Size = UDim2.fromOffset(500, 520),
    Transparent = true,
    Theme = "Dark",
    Resizable = true,
    SideBarWidth = 200,
    HideSearchBar = true,
    ScrollBarEnabled = false,
})

local MainTab = Window:Tab({ Title = "Azure Lock", Icon = "home" })

-- =====================================================
--  SEÇÃO AIMBOT
-- =====================================================
local secAimbot = MainTab:Section({ Title = "Aimbot", TextXAlignment = "Left" })

secAimbot:Toggle({
    Title = "Aimbot Personagem (Grudado)",
    Desc = "Corpo vira para a bola",
    Callback = function(v)
        ativarAimbotPersonagem(v)
    end
})

secAimbot:Toggle({
    Title = "Aimbot Câmera (Estilo antigo)",
    Desc = "Câmera atrás do personagem olhando para a bola",
    Callback = function(v)
        aimbotCamera = v
        if cameraToggleButton then
            if v then
                cameraToggleButton.BackgroundColor3 = Color3.fromRGB(40, 167, 69)
                cameraToggleButton.Text = "Aimbot Cam [ON]"
                cameraButtonActive = true
            else
                cameraToggleButton.BackgroundColor3 = Color3.fromRGB(220, 53, 69)
                cameraToggleButton.Text = "Aimbot Cam [OFF]"
                cameraButtonActive = false
            end
        end
    end
})

secAimbot:Toggle({
    Title = "Botão Voador (Câmera)",
    Desc = "Cria um botão flutuante para ativar/desativar a câmera",
    Callback = function(v)
        if v then
            criarBotaoCameraFlutuante()
        else
            removerBotaoCameraFlutuante()
        end
    end
})

secAimbot:Slider({
    Title = "Tempo do Aimbot (Delay)",
    Desc = "0=0s | 1=0.1s | 2=0.2s | 3=0.25s | 4=0.40s | 5=0.60s | 6=0.70s | 7=1s | 8=2s | 9=3s | 10=5s",
    Step = 1,
    Value = { Min = 0, Max = 10, Default = 0 },
    Callback = function(v)
        aimbotDelayValue = aimbotDelayValues[v] or 0
    end
})

-- =====================================================
--  SEÇÃO VISÃO DE JOGO
-- =====================================================
local secVisao = MainTab:Section({ Title = "Visão de jogo", TextXAlignment = "Left" })

secVisao:Toggle({
    Title = "Visão da bola (Linha Verde)",
    Desc = "Linha verde gigante (100 studs) saindo da cabeça do inimigo com a bola",
    Callback = function(v)
        visaoBolaEnabled = v
        if not v and linhaVisao then
            linhaVisao:Destroy()
            linhaVisao = nil
        end
    end
})

secVisao:Toggle({
    Title = "Advinha Chute (Linha + Círculo Amarelo)",
    Desc = "Trajetória da bola (linha grossa) e círculo amarelo no chão",
    Callback = function(v)
        advinhaChuteEnabled = v
        if not v then
            if linhaChute then linhaChute:Destroy() linhaChute = nil end
            if circuloChute then circuloChute:Destroy() circuloChute = nil end
        end
    end
})

-- =====================================================
--  SEÇÃO DRIBLE
-- =====================================================
local secDrible = MainTab:Section({ Title = "Drible", TextXAlignment = "Left" })

secDrible:Toggle({
    Title = "Drible Ativo",
    Desc = "Ativa o círculo rosa de detecção e executa drible",
    Callback = function(v)
        dribleEnabled = v
        if v then
            setupDrible()
        else
            if dribleSphere then
                dribleSphere.Transparency = 1
            end
        end
    end
})

pcall(function()
    game:GetService("StarterGui"):SetCore("SendNotification",{
        Title = "Azure Lock",
        Text = "Linha amarela corrigida: funciona em qualquer situação!",
        Duration = 4
    })
end)
