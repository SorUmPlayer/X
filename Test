--[[
    TESTE DE DROPDOWNS – WindUI
]]

local WindUI = loadstring(game:HttpGet("https://github.com/Footagesus/WindUI/releases/latest/download/main.lua"))()

local Window = WindUI:CreateWindow({
    Title = "🧪 Teste de Dropdowns",
    Icon = "rbxassetid://10723343321",
    Author = "Sor_Gostoso",
    Folder = "DropdownTest",
    Size = UDim2.fromOffset(450, 500),
    Transparent = true,
    Theme = "Dark",
    Resizable = true,
    SideBarWidth = 200,
    HideSearchBar = true,
    ScrollBarEnabled = false,
})

local Tab = Window:Tab({ Title = "Teste", Icon = "list" })

-- =====================================================
--  SEÇÃO: DROPDOWNS
-- =====================================================
local Section = Tab:Section({ Title = "Dropdowns de Teste", TextXAlignment = "Left" })

-- 1. Dropdown com opções simples (strings)
local dropdown1
dropdown1 = Section:Dropdown({
    Title = "Dropdown 1 – Cores",
    Desc = "Selecione uma cor",
    Options = {"Vermelho", "Verde", "Azul", "Amarelo", "Roxo"},
    Callback = function(opt)
        print("Dropdown 1 selecionou: " .. opt)
    end
})

-- 2. Dropdown com opções numéricas
local dropdown2
dropdown2 = Section:Dropdown({
    Title = "Dropdown 2 – Números",
    Desc = "Selecione um número",
    Options = {"10", "20", "30", "40", "50"},
    Callback = function(opt)
        print("Dropdown 2 selecionou: " .. opt)
    end
})

-- 3. Dropdown com opções mistas (com números e textos)
local dropdown3
dropdown3 = Section:Dropdown({
    Title = "Dropdown 3 – Direções (teste)",
    Desc = "Escolha uma direção",
    Options = {"Norte", "Sul", "Leste", "Oeste", "Centro"},
    Callback = function(opt)
        print("Dropdown 3 selecionou: " .. opt)
    end
})

-- 4. Dropdown com opções de time (Home/Away)
local dropdown4
dropdown4 = Section:Dropdown({
    Title = "Dropdown 4 – Times",
    Desc = "Escolha um time",
    Options = {"Home", "Away"},
    Callback = function(opt)
        print("Dropdown 4 selecionou: " .. opt)
    end
})

-- 5. Dropdown com opções de curva (o que você usava)
local dropdown5
dropdown5 = Section:Dropdown({
    Title = "Dropdown 5 – Curva",
    Desc = "Direção da curva",
    Options = {"Direita", "Esquerda", "Aleatório"},
    Callback = function(opt)
        print("Dropdown 5 selecionou: " .. opt)
    end
})

-- =====================================================
--  SEÇÃO: INFORMAÇÕES
-- =====================================================
local InfoSection = Tab:Section({ Title = "Informações", TextXAlignment = "Left" })

local infoLabel = InfoSection:Label({
    Title = "Instruções",
    Desc = "Teste cada dropdown abaixo.\nSe algum não mostrar opções, anote qual.\n\nSe TODOS não funcionarem, o problema é no executor ou na versão do WindUI."
})

-- =====================================================
--  BOTÃO PARA MOSTRAR VALORES SELECIONADOS
-- =====================================================
local function mostrarValores()
    print("=== VALORES DOS DROPDOWNS ===")
    -- Nota: não temos como ler o valor atual diretamente, mas você pode ver no console ao selecionar.
    print("Teste feito! Veja os prints acima.")
end

InfoSection:Button({
    Title = "Ver valores no console",
    Desc = "Clique e veja os valores no console",
    Callback = function()
        mostrarValores()
    end
})

pcall(function()
    game:GetService("StarterGui"):SetCore("SendNotification",{
        Title = "Teste de Dropdowns",
        Text = "Teste cada dropdown e veja se as opções aparecem!",
        Duration = 5
    })
end)
